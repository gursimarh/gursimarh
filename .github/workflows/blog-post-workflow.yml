name: "📚 latest Blog"

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */24 * * *" # Runs Every 24 Hours

jobs:
  update_blogs:
    name: "Update With Latest Blogs"
    runs-on: ubuntu-latest
    steps:
      - name: "📥  Fetching Repository Contents"
        uses: actions/checkout@main

      - name: "📚  Medium Updater"
        uses: "gursimarh/blog-post-workflow@master"
        with:
          USERNAME: "" # Username
          COUNT: 4 # MAX Visisble
          STYLE: "blog-left"
          BLOG_URL: "https://gursimar27.medium.com/"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
